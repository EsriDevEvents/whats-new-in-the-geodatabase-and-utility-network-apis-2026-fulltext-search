<UserControl x:Class="FullTextSearchDevSummitDemo.SearchDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:ui="clr-namespace:FullTextSearchDevSummitDemo"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{Binding Path=ui.SearchDockPaneViewModel}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <ui:BooleanToRadioConverter x:Key="BooleanToRadioConverter"></ui:BooleanToRadioConverter>
        </ResourceDictionary>        
    </UserControl.Resources>
    <Grid Margin="10" >
        <Grid.Resources>
            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Margin" Value="0,0,0,4" />
            </Style>
            <Style TargetType="{x:Type ComboBox}">
                <Setter Property="Margin" Value="0,0,0,4" />
            </Style>
            <Style TargetType="{x:Type Button}">
                <Setter Property="Margin" Value="0,0,0,4" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Dataset" Grid.Row="0" Grid.Column="0"/>
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Datasets}" SelectedItem="{Binding SelectedDataset}" ></ComboBox>

        <Label Content="Fields" Grid.Row="1" Grid.Column="0"/>
        <ListBox Grid.Row="1" Grid.Column="1"
                 ItemsSource="{Binding DatasetFields}"
                 DisplayMemberPath="FieldName"
                 SelectionMode="Multiple">
            <ListBox.ItemContainerStyle>
                <!-- This Style binds a ListBoxItem to a the ViewModelItem. -->
                <Style TargetType="ListBoxItem">
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        
        <Label Content="Search Terms" Grid.Row="2" Grid.Column="0"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SearchTerm, Mode=TwoWay}" ></TextBox>
        
        <StackPanel Grid.Row="4" Grid.Column="0" HorizontalAlignment="Stretch" Orientation="Horizontal">
            <!-- Bind the IsChecked property of the "True" button to your boolean property -->
            <RadioButton Content="OR" IsChecked="{Binding SelectedRadio,Converter={StaticResource  BooleanToRadioConverter}, ConverterParameter=True}" Margin="4" GroupName="BooleanGroup"/>

            <!-- The "False" button can be left unbound as the GroupName handles mutual exclusion -->
            <RadioButton Content="AND" IsChecked="{Binding SelectedRadio, Converter={StaticResource  BooleanToRadioConverter}, ConverterParameter=False}" GroupName="BooleanGroup" />           
        </StackPanel>

        <Label Content="Search Terms" Grid.Row="5" Grid.Column="0"/>
        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding SearchExpression, Mode=TwoWay}" ></TextBox>

        <Button Content="Search" Grid.Row="6" Grid.Column="1" Margin="10" Command="{Binding SearchCommand}"></Button>
        <TextBlock Background="Cornsilk" Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2"  MinHeight="100px" Text="{Binding SearchResult,Mode=TwoWay}"/>
    </Grid>
</UserControl>